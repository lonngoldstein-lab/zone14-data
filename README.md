# Zone 14 data

One file: `current.json` — the live matchweek data the page fetches.
Block names at the top level, plus `matchweek`, `snapshots`, `updated`.
Overwritten whole by each scheduled run. No markup here.
